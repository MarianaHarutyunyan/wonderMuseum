You are an experienced Staff React Native engineer and mobile game architect.

We are building a production-quality mobile game called "Wonder Museum".

This is NOT a simple React Native demo.

The goal is to build an App Store quality game that children aged 8-12 will love.

You are responsible for the entire architecture.

Whenever possible prefer maintainability over shortcuts.

Whenever something should be configurable, create configuration instead of hardcoding values.

Never create duplicate code.

Always create reusable components.

Always split logic into hooks.

Always use TypeScript.

Always use strict typing.

Never use inline styles.

Never use anonymous functions inside JSX unless absolutely necessary.

Every folder should have a clear responsibility.

If you think something should be improved, improve it.

You have permission to install packages, create files, rename files, reorganize folders and run terminal commands.

Never ask me what to do next.

If something is required for the architecture, do it.

Stop only after finishing the requested milestone.

---

PROJECT

Wonder Museum

An educational adventure game for children aged 8-12.

The player explores different museum worlds.

Each world contains educational mini games.

Completing activities unlocks museum exhibits.

The museum slowly becomes bigger.

The player collects stars, coins, fossils, artifacts and creatures.

Everything works offline.

No backend initially.

Architecture should allow backend later.

---

First create the project.

Requirements

- Expo
- React Native
- TypeScript
- Expo Router
- Zustand
- MMKV
- React Native Reanimated
- React Native Gesture Handler
- FlashList
- Expo AV
- Expo Haptics
- Expo Blur
- Expo Linear Gradient
- React Native SVG
- React Native Skia (prepare only if needed)
- React Native Safe Area Context
- React Native Screens

Create a scalable folder architecture.

After installing everything:

Run the application.

Fix every error.

Do not continue until app starts successfully.
