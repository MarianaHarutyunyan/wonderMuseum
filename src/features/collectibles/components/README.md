# features/collectibles/components/

UI specific to displaying collectibles.

- `CollectibleBadge` — icon-less badge showing a collectible label + count.
- `CollectibleSummaryBar` — horizontal bar of all `CollectibleBadge`s, driven by
  `src/content/rewards` (types) and `src/store/progressStore` (counts).
