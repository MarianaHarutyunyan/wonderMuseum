# content/questions/

Reserved for the quiz/question bank consumed by quiz-type mini-games (see
`MiniGameType = 'quiz'` in `src/types/world.types`).

## Status

Intentionally empty. No gameplay has been built yet. When populated, each world's quiz
mini-game will reference a question set defined here rather than embedding questions inline
in `src/content/worlds`.
