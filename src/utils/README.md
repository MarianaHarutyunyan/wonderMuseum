# utils/

Small, pure, stateless helper functions with no React and no side effects — the kind of
function that takes an input and returns an output, testable in isolation.

## Status

Intentionally empty right now. Feature-specific helpers (like `getWorldById`) live in their
feature's own `utils/` folder until at least two features need the same helper, at which
point it should move here.
