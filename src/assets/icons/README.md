# assets/icons/

Small UI/iconography assets (collectible icons, world icons, navigation glyphs). Reserved and
currently empty. Prefer `react-native-svg` components for icons that need to be
recolored/resized; use this folder for icons that only make sense as fixed raster/SVG files.
